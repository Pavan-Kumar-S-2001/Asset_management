version: "3.9"

services:

  backend:
    build: ./backend
    container_name: asset_backend
    ports:
      - "5000:5000"
    

  frontend:
    build: ./frontend
    container_name: asset_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend